fun main() {
    val class1 = readln().toInt()
    val class2 = readln().toInt()
    val class3 = readln().toInt()
    println(class1 / 2 + class1 % 2 + class2 / 2 + class2 % 2 + class3 / 2 + class3 % 2)
}
