fun main() {
    var invitation = readLine().toBoolean()
    var present = readLine().toBoolean()// read other value in the same way
    if (invitation==true && present==true){
        println("true")
    }
    else {
        println("false")
    }
}